<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/static/css/style.css">
</head>

<body>
    <div id="container">
        <div>
        {{#if errors }}
        {{#each errors}}
        <div class="notification errorBox">
            <span>{{this}}</span>
        </div>
        {{/each}}
        {{/if}}
        </div>

        {{!-- <div>
            <div class="notification loadingBox">
                Loading &hellip;
            </div>
            <div class="notification infoBox">
                <span>Info</span>
            </div>

        </div> --}}

        <nav>
            <div class="left-container">
                <ul>
                    <li><a href="/">Home</a></li>
                    {{#if username}}
                    <li><a href="/hotels/create">Add +</a></li>
                    {{else}}
                    <li><a href="/accounts/login">Login</a></li>
                    <li><a href="/accounts/register">Register</a></li>
                    {{/if}}                    

                </ul>
            </div>
            <div class="right-container">
                {{#if username}}
                <a href="/accounts/profile" class="log-out">{{username}}</a>
                <a href="/accounts/logout" class="log-out">Logout</a>
                {{/if}}                
            </div>
        </nav>

        {{{ body }}}

        <footer>@SoftUni - JS Back-end - BookingUni 2022</footer>
    </div>

</body>

</html>