<section>
    <h1>{{title}}</h1>

    {{#if errors}}
    <div class="error-box align-center">
        {{#each errors}}
        <p>{{this}}</p>
        {{/each }}
    </div>
    {{/if}}

    <div class="item padded align-center">
        <form class="layout left large" action="/auth/login" method="post">

            <div class="col aligned">
                <label for="username">
                    <span>Username</span>
                    <input class="padded" type="text" name="username" id="username" />
                </label>
                <label for="password">
                    <span>Password</span>
                    <input class="padded" type="password" name="password" id="password" />
                </label>
            </div>

            <div class="content pad-med align-center vertical pt-20 ">
                <div class="align-center">
                    <input class="action btn-big" type="submit" value="Login"/>
                </div>
            </div>
        </form>
    </div>
</section>