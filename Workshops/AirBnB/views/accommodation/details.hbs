<section>
    {{#with room}}
    <h1>{{name}} - Details</h1>

    <div>
        <img src="{{imageUrl}}" alt="{{name}}">
    </div>
    
    <p>{{description}}</p>
    <p>
        <strong>Price per night: {{price}}€</strong>
    </p>
    {{/with}}
</section>